<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.appmoviesseries">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo_filmes_e_series"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo_filmes_e_series"
        android:supportsRtl="true"
        android:theme="@style/Theme.APPMoviesSeries">
        <activity
            android:name=".FormListaFilmes"
            android:label="@string/app_name">
        </activity>
        <activity android:name=".FormCadastroFilmes" />
        <activity android:name=".FormPrincipal" />
        <activity android:name=".PerfilUsuario" />
        <activity android:name=".FormCadastro" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name" />
        <activity android:name=".FormLogin">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>